#!/bin/sh

minutes=${1}
gracePeriod=${2}

var=$(./list_unused_files.sh ${minutes})
#echo $var

notify-send -t 5000 "These files will be deleted in ${gracePeriod} minutes:" "$(list_unused_files.sh ${minutes})"

